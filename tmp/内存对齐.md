# 简述

通过在数据结构中填充字节的方式来确保数据的起始位置为特定边界。可以提高内存的访问速度，保证硬件兼容性

# 基本规则

- 数据结构的总大小应该是其最宽基本数据类型成员大小的整数倍
- 数据的起始地址应该是其自身类型大小的整数倍
- 如果结构体的成员是类类型，那么这个类类型的内存对齐应该按照其内部成员拆分为基础类型来考虑。这意味着类内部的成员也必须遵循内存对齐规则
- 内存的排列顺序与代码中的定义顺序一致