以下是一个常见的流程示例，展示了在一个新初始化（`git init`）的本地仓库中，添加远程仓库并拉取（checkout）指定远程分支、同时让本地分支跟踪该远程分支的做法。

---

## 1. 在本地仓库中初始化并添加远程

```bash
# （1）初始化本地仓库
git init

# （2）添加远程仓库，通常命名为 origin
git remote add origin <远程仓库地址>
```

---

## 2. 拉取并关联指定分支

### 方法一：通过 fetch + checkout 的方式

1. **从远程仓库获取所有分支信息：**
   
   ```bash
   git fetch origin
   ```
   
   这一步会把远程的所有分支信息更新到本地，但并不会将它们合并到本地分支。

2. **创建并切换到本地新分支，直接关联远程指定分支：**
   
   ```bash
   # 假设远程分支名字叫 feature/awesome
   # 下面命令会在本地创建一个同名分支，并将其跟踪 origin/feature/awesome
   git checkout -b feature/awesome origin/feature/awesome
   ```
   
   这样本地分支 `feature/awesome` 就跟踪了远程分支 `origin/feature/awesome`。  
   如果需要别的本地分支名字，可以改成：
   
   ```bash
   git checkout -b local-branch-name origin/feature/awesome
   ```

3. **可选：更新本地分支到最新提交：**
   
   ```bash
   git pull
   ```
   
   如果是刚创建完立即checkout的分支，通常已经是最新了；但是如果觉得需要再同步，可以执行这一步。

---

### 方法二：通过 pull 直接创建/更新本地分支

1. **直接从远程分支 pull 到一个新的/已有的本地分支：**
   
   ```bash
   # 以拉取远程的 feature/awesome 分支为例
   # 如果本地还没有同名分支，可以这样：
   git pull origin feature/awesome:feature/awesome
   ```
   
   这条命令在本地创建了 `feature/awesome` 分支并把远程的同名分支的代码拉取下来。之后可使用
   
   ```bash
   git checkout feature/awesome
   ```
   
   切换到该分支进行开发。

2. **手动设置跟踪（如果没自动设置成功）：**
   
   如果上面的操作没有自动建立跟踪关系，可以用以下命令手动建立跟踪关系：
   
   ```bash
   git branch --set-upstream-to=origin/feature/awesome feature/awesome
   ```

---

### 方法三：在已经创建好的本地分支上设置跟踪

如果你**先**创建了一个本地分支 `mybranch`，并想让它跟踪远程的 `origin/feature/awesome`，可以这样：

1. 确保本地分支处于最新或为空仓库初始化后状态（也可以先 `git checkout mybranch` 切换到该分支）。

2. 运行下面命令建立关联：
   
   ```bash
   git branch --set-upstream-to=origin/feature/awesome mybranch
   ```

3. 再 pull：
   
   ```bash
   git pull
   ```
   
   之后本地的 `mybranch` 就会跟踪 `origin/feature/awesome`。

---

## 3. 验证分支跟踪状态

- 使用 `git branch -vv` 可以查看当前所有本地分支和它们对应的远程分支。

- 如果在后续需要向远程提交此分支，可以使用：
  
  ```bash
  git push
  ```
  
  如果是首次 push 并且还没有建立对应关系，可以加上 `-u` 参数（等同于 `--set-upstream`），如：
  
  ```bash
  git push -u origin feature/awesome
  ```
  
  这样以后再推送同分支，只需要 `git push` 就行了。

---

以上就是在一个新初始化的仓库中，添加远程仓库并且拉取指定远程分支，创建本地分支并让其跟踪远程分支的常用操作步骤。总结一下核心命令：

1. `git init`
2. `git remote add origin <远程地址>`
3. `git fetch origin`
4. `git checkout -b <本地分支名> origin/<远程分支名>`  
   （或 `git pull origin <远程分支>:<本地分支>`）
5. （必要时）`git branch --set-upstream-to=origin/<远程分支> <本地分支>`
